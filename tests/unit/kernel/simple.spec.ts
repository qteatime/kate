import { describe } from "../util";

describe("@kernel button-based input", ({ test }) => {
  test("Pressing buttons", async ({ kate }) => {
    const buttons = new kate.kernel.KateButtons();
  });
});
